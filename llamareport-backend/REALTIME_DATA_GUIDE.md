# 实时数据功能使用指南

## 📋 目录

- [功能概述](#功能概述)
- [快速开始](#快速开始)
- [配置说明](#配置说明)
- [API 接口文档](#api-接口文档)
- [Agent 使用示例](#agent-使用示例)
- [数据源说明](#数据源说明)
- [故障排除](#故障排除)

---

## 功能概述

### ✨ 核心特性

实时数据功能为 LlamaReport Backend 添加了以下能力:

1. **实时股价查询** - 获取股票最新价格、涨跌幅、成交量等
2. **财经新闻追踪** - 追踪公司最新新闻动态
3. **公司公告获取** - 获取官方公告、业绩预告等
4. **智能预警检测** - 自动检测价格异常、成交量异常等
5. **市场概览** - 获取主要指数实时情况

### 🎯 应用场景

```
场景 1: 投资决策
用户问: "贵州茅台现在值得投资吗？"
系统:
├── 年报分析: 历史业绩优秀，ROE 持续提升
├── 实时数据: 当前股价处于低位，PE 仅 25 倍
├── 最新新闻: 刚发布Q3业绩，营收增长15%
└── 综合建议: 估值合理，可以关注

场景 2: 风险监控
用户: "帮我监控持仓股票，发现异常及时提醒"
系统:
├── 实时监控股价波动
├── 追踪重大公告
├── 分析新闻情绪
└── 异常时推送预警

场景 3: 综合分析
用户: "给我做一份 XX 公司的完整分析报告"
系统:
├── 历史分析: 从年报提取3年数据
├── 现状分析: 从实时数据看当前状况
├── 动态追踪: 最新新闻和公告
└── 生成报告: 历史 + 现状 + 未来预期
```

---

## 快速开始

### 1. 安装依赖

```bash
cd llamareport-backend
pip install -r requirements.txt
```

新增依赖包:
- `tushare` - Tushare Pro API（可选，但推荐）
- `requests` - HTTP 请求
- `beautifulsoup4` - 网页解析
- `lxml` - XML/HTML 解析

### 2. 配置环境变量

创建或编辑 `.env` 文件:

```env
# 必需配置
OPENAI_API_KEY=sk-proj-xxx          # 用于 Embedding
DEEPSEEK_API_KEY=sk-xxx             # 用于对话模型

# 实时数据配置（可选）
TUSHARE_API_TOKEN=your_token_here   # Tushare API Token（可选）
ENABLE_REALTIME_DATA=true           # 是否启用实时数据功能

# 其他配置
DEBUG=false
```

### 3. 获取 Tushare Token（推荐但可选）

**方式 1: 使用 Tushare（推荐）**

1. 访问 https://tushare.pro/register
2. 注册账号（免费）
3. 获取 API Token
4. 将 Token 添加到 `.env` 文件

**方式 2: 不使用 Tushare**

如果不配置 Tushare Token，系统会自动使用新浪财经作为数据源。
新浪财经免费且无需 Token，但数据范围相对有限。

### 4. 启动服务

```bash
# 方式 1: 使用启动脚本
python start.py

# 方式 2: 直接启动
python main.py

# 方式 3: 使用 uvicorn
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 5. 验证功能

访问 API 文档查看新增接口:

```bash
http://localhost:8000/docs
```

在文档中找到 `/realtime` 相关的接口。

---

## 配置说明

### 环境变量详解

| 变量名 | 说明 | 默认值 | 必需 |
|--------|------|--------|------|
| `TUSHARE_API_TOKEN` | Tushare Pro API Token | - | ❌ 可选 |
| `ENABLE_REALTIME_DATA` | 是否启用实时数据功能 | `true` | ❌ |

### 数据源优先级

系统会按以下优先级使用数据源:

1. **实时行情**: 新浪财经 → Tushare
2. **新闻**: 新闻聚合器（财联社、新浪财经等）
3. **公告**: Tushare → 巨潮资讯

**说明**:
- 新浪财经: 免费、实时性好、无需 Token
- Tushare: 需要 Token、数据更全面、支持历史数据
- 如果某个数据源失败，会自动尝试备用数据源

---

## API 接口文档

### 1. 获取实时股价

```http
GET /realtime/quote/{stock_code}
```

**参数**:
- `stock_code`: 股票代码（如 `600000.SH` 或 `000001.SZ`）

**示例**:
```bash
curl http://localhost:8000/realtime/quote/600000.SH
```

**响应**:
```json
{
  "status": "success",
  "stock_code": "600000.SH",
  "data": "📊 **浦发银行 (600000.SH)** 实时行情\n\n💰 **价格信息**\n- 最新价: 8.52 元...",
  "timestamp": "2024-01-15T10:30:00"
}
```

### 2. 获取公司新闻

```http
POST /realtime/news
Content-Type: application/json

{
  "company_name": "贵州茅台",
  "limit": 10
}
```

**示例**:
```bash
curl -X POST http://localhost:8000/realtime/news \
  -H "Content-Type: application/json" \
  -d '{"company_name": "贵州茅台", "limit": 5}'
```

### 3. 获取公司公告

```http
POST /realtime/announcements
Content-Type: application/json

{
  "stock_code": "600519.SH",
  "limit": 10
}
```

### 4. 检查股票预警

```http
GET /realtime/alerts/{stock_code}
```

**示例**:
```bash
curl http://localhost:8000/realtime/alerts/600519.SH
```

### 5. 获取市场概览

```http
GET /realtime/market/overview
```

**示例**:
```bash
curl http://localhost:8000/realtime/market/overview
```

### 6. 健康检查

```http
GET /realtime/health
```

检查实时数据服务和各数据源的健康状态。

### 7. 获取统计信息

```http
GET /realtime/statistics
```

获取各数据源的请求统计、成功率等信息。

---

## Agent 使用示例

### 示例 1: 查询实时股价

```bash
curl -X POST http://localhost:8000/agent/query \
  -H "Content-Type: application/json" \
  -d '{
    "question": "帮我查一下贵州茅台现在的股价"
  }'
```

**Agent 工作流程**:
1. 识别用户需求: 查询实时股价
2. 选择工具: `get_realtime_stock_price`
3. 执行查询: 获取茅台实时行情
4. 返回结果: 格式化的股价信息

### 示例 2: 综合投资分析

```bash
curl -X POST http://localhost:8000/agent/query \
  -H "Content-Type: application/json" \
  -d '{
    "question": "贵州茅台值得投资吗？给我做个综合分析"
  }'
```

**Agent 工作流程**:
1. 使用 `get_realtime_stock_price` → 获取当前价格和估值
2. 使用 `annual_report_query` → 查询年报业绩
3. 使用 `get_latest_financial_news` → 查看最新动态
4. 使用 `check_stock_alerts` → 检查异常情况
5. 综合分析 → 给出投资建议

### 示例 3: 新闻追踪

```bash
curl -X POST http://localhost:8000/agent/query \
  -H "Content-Type: application/json" \
  -d '{
    "question": "贵州茅台最近有什么重要新闻吗？"
  }'
```

### 示例 4: 风险检查

```bash
curl -X POST http://localhost:8000/agent/query \
  -H "Content-Type: application/json" \
  -d '{
    "question": "帮我检查一下中国平安有没有异常情况"
  }'
```

---

## 数据源说明

### 1. Tushare (推荐)

**优势**:
- ✅ 数据全面: 覆盖股票、基金、期货等
- ✅ 历史数据: 支持查询历史行情
- ✅ 财务指标: 提供详细的财务指标数据
- ✅ 公告数据: 支持公司公告查询

**劣势**:
- ⚠️ 需要注册: 需要获取 API Token
- ⚠️ 有限制: 免费版有调用次数限制

**适用场景**: 需要全面数据、历史数据、财务分析

**获取方式**: https://tushare.pro/register

### 2. 新浪财经

**优势**:
- ✅ 完全免费: 无需 Token
- ✅ 实时性好: 数据更新及时
- ✅ 稳定可靠: 服务稳定

**劣势**:
- ⚠️ 数据有限: 主要是实时行情
- ⚠️ 无历史数据: 不支持历史查询

**适用场景**: 实时行情查询、快速验证

**说明**: 系统会自动使用，无需配置

### 3. 新闻聚合器

**数据来源**:
- 财联社: https://www.cls.cn
- 新浪财经: https://finance.sina.com.cn
- 东方财富: https://finance.eastmoney.com

**说明**: 
- 当前为示例实现，返回模拟数据
- 生产环境需要实现真实的爬虫逻辑
- 建议使用专业的新闻 API 服务

### 4. 巨潮资讯网

**用途**: 获取公司公告

**说明**:
- 官方权威数据源
- 免费公开数据
- 包含所有上市公司公告

---

## 故障排除

### 问题 1: "Tushare package not installed"

**原因**: 未安装 tushare 包

**解决**:
```bash
pip install tushare
```

### 问题 2: "TUSHARE_API_TOKEN not provided"

**原因**: 未配置 Tushare Token

**解决**:
- 方案 1: 获取 Token 并配置到 `.env`
- 方案 2: 不配置 Token，系统会自动使用新浪财经

### 问题 3: "No realtime data for XXX"

**原因**: 股票代码格式不正确或股票停牌

**解决**:
- 检查股票代码格式: `600000.SH` (上海) 或 `000001.SZ` (深圳)
- 确认股票是否停牌
- 尝试使用不同的数据源

### 问题 4: "Request timeout"

**原因**: 网络问题或 API 服务不可用

**解决**:
- 检查网络连接
- 查看 `/realtime/health` 检查数据源状态
- 稍后重试

### 问题 5: API 调用失败率高

**解决**:
1. 查看统计信息: `GET /realtime/statistics`
2. 检查数据源健康: `GET /realtime/health`
3. 查看日志: `llamareport-backend.log`
4. 考虑配置备用数据源

---

## 高级用法

### 1. Python 代码中使用

```python
from agents.realtime_tools import (
    get_realtime_stock_price,
    get_latest_financial_news,
    check_stock_alerts
)

# 获取实时股价
price_info = get_realtime_stock_price("600519.SH")
print(price_info)

# 获取最新新闻
news = get_latest_financial_news("贵州茅台", limit=5)
print(news)

# 检查预警
alerts = check_stock_alerts("600519.SH")
print(alerts)
```

### 2. 自定义数据源

创建自己的数据源适配器:

```python
from data_sources.base import BaseDataSource

class MyCustomDataSource(BaseDataSource):
    def __init__(self):
        super().__init__("MyCustomSource")
    
    def initialize(self) -> bool:
        # 初始化逻辑
        self.is_initialized = True
        return True
    
    def get_realtime_quote(self, stock_code: str):
        # 实现获取实时行情的逻辑
        pass
    
    def get_company_news(self, company_name: str, limit: int = 10):
        # 实现获取新闻的逻辑
        pass
    
    def get_announcements(self, stock_code: str, limit: int = 10):
        # 实现获取公告的逻辑
        pass
```

### 3. 添加新工具到 Agent

```python
from llama_index.core.tools import FunctionTool
from agents.realtime_tools import create_realtime_data_tools

def my_custom_tool(param: str) -> str:
    """自定义工具"""
    return f"Result for {param}"

# 创建工具
custom_tool = FunctionTool.from_defaults(
    fn=my_custom_tool,
    name="my_custom_tool",
    description="这是一个自定义工具"
)

# 添加到 Agent
realtime_tools = create_realtime_data_tools()
all_tools = realtime_tools + [custom_tool]
```

---

## 性能优化建议

### 1. 使用缓存

对于不常变化的数据（如公司基本信息），可以添加缓存:

```python
from functools import lru_cache

@lru_cache(maxsize=100)
def get_stock_basic_info(stock_code: str):
    # 获取基本信息
    pass
```

### 2. 批量请求

如果需要查询多只股票，建议使用批量接口:

```python
# 不推荐
for code in stock_codes:
    get_realtime_stock_price(code)

# 推荐
get_multiple_stocks_price(stock_codes)  # 实现批量查询
```

### 3. 异步处理

对于耗时的操作，使用异步处理:

```python
import asyncio

async def get_comprehensive_data(stock_code: str):
    # 并行获取多种数据
    tasks = [
        get_quote_async(stock_code),
        get_news_async(stock_code),
        get_announcements_async(stock_code)
    ]
    results = await asyncio.gather(*tasks)
    return results
```

---

## 更新日志

### v1.0.0 (2024-01-15)

**新增功能**:
- ✅ 实时股价查询
- ✅ 财经新闻追踪
- ✅ 公司公告获取
- ✅ 智能预警检测
- ✅ 市场概览

**数据源支持**:
- ✅ Tushare Pro
- ✅ 新浪财经
- ✅ 新闻聚合器
- ✅ 巨潮资讯

**Agent 集成**:
- ✅ 5个实时数据工具
- ✅ 智能工具选择
- ✅ 历史与实时数据联动

---

## 常见问题 FAQ

### Q1: 实时数据功能是否需要付费？

**A**: 基础功能完全免费。
- 新浪财经: 免费，无需注册
- Tushare 免费版: 免费，需要注册，有调用限制
- Tushare 付费版: 500-1000元/月，无限制

### Q2: 数据延迟多少？

**A**: 
- 新浪财经: < 3 秒
- Tushare: < 5 秒
- 新闻: < 1 分钟（爬虫实时更新）

### Q3: 支持哪些市场？

**A**: 
- ✅ 上海证券交易所（沪市）
- ✅ 深圳证券交易所（深市）
- ✅ 北京证券交易所（北交所）

### Q4: 如何提高数据准确性？

**A**:
1. 配置 Tushare Token（官方数据更准确）
2. 使用多数据源交叉验证
3. 关注数据更新时间

### Q5: Agent 如何决定使用哪个工具？

**A**: 
Agent 基于 LlamaIndex FunctionAgent，会根据:
1. 用户问题的语义
2. 工具的描述信息
3. 历史调用效果
自动选择最合适的工具。

---

## 技术支持

### 文档

- [项目 README](./README.md)
- [Agent 系统文档](./AGENT_README.md)
- [API 文档](http://localhost:8000/docs)

### 日志

查看详细日志:
```bash
tail -f llamareport-backend.log
```

### 联系方式

如有问题请提交 Issue 或联系开发团队。

---

**版本**: 1.0.0  
**最后更新**: 2024-01-15  
**状态**: ✅ 已完成
